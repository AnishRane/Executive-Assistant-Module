node_modules/
dist/
build/
.turbo/
.cache/
coverage/
*.log
.DS_Store
.env
.env.local
.env.*.local

# Build incremental caches
*.tsbuildinfo

# Debug captures / scratch
*.har
*.har.json
ea-network.txt.har
report-*.html
conflict-resolve-issue.json

# Version-history scratch artifacts
module.v[0-9]*.json
new-v[0-9.]*.json

# Internal scratch inside docs/ — only docs/DESIGN.md, docs/OPERATIONS.md,
# docs/ROADMAP.md are public. Everything else under docs/ is workspace-only
# (brainstorm notes, HTML mockups, debug investigations).
docs/_brainstorm/
docs/*.html
docs/.test-report.json
docs/generate_test_report.mjs
docs/categorization-design.md
docs/conflict-detection-investigation.md
docs/ui_refactor_design.md

# Internal planning doc kept out of public release.
/PHASE_PLAN.md

# Root-level versions of docs (moved to docs/ in v0.1.0). Prevent
# accidental re-add at root.
/DESIGN.md
/OPERATIONS.md
/ROADMAP.md
